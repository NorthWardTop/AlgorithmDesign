#include <iostream>
#include <iomanip>
using namespace std;
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	unsigned __int64 in=0;

	cin>>in;
	cout<<setiosflags(ios::uppercase)<<hex<<in;
	
	
	return 0;
}
