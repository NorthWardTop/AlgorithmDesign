#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

#define MAX(n) ((n*2)-1)
int arr[MAX(34)][MAX(34)]={0};
void retree(n)
{
	if()
}

int main(int argc, char *argv[]) {
	
	
	
	return 0;
}
